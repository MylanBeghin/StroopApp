<UserControl x:Class="StroopApp.Views.Configuration.Profile.ProfileSettingRow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:StroopApp.Views.Configuration.Profile"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Border CornerRadius="4" Background="Lavender">
        <Grid MinHeight="80">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="12"/>
                <ColumnDefinition Width="0.6*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="12"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition MinHeight="80"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                <TextBlock FontSize="16" TextWrapping="Wrap"
                           Text="{Binding Header, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
                <TextBlock FontSize="12" TextWrapping="Wrap"
                           Text="{Binding Description, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
            </StackPanel>
            <ContentPresenter Grid.Column="2" VerticalAlignment="Center"
                              Content="{Binding SettingContent, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
            <ContentPresenter Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="2"
                      Content="{Binding DetailContent, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
        </Grid>
    </Border>
</UserControl>

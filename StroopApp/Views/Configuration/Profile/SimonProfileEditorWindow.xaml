<Window x:Class="StroopApp.Views.Configuration.Profile.SimonProfileEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:StroopApp.Views.Configuration.Profile"
        xmlns:ui="http://schemas.modernwpf.com/2019"
        mc:Ignorable="d"
        Title="SimonProfileEditorWindow" Height="700" Width="800">
    <Window.Resources>
        <SolidColorBrush x:Key="OpaqueDefaultButtonBackground"
                 Color="#c4c4c4"
                 Opacity="1" />
    </Window.Resources>
    <Grid >
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <ScrollViewer>
            <ui:SimpleStackPanel Margin="16,16,16,64" Spacing="16" >
                <local:ProfileEditorHeaderView/>
                <Border CornerRadius="4" BorderBrush="Gray" BorderThickness="1">
                    <Grid  Height="80" >
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="12"/>
                            <ColumnDefinition Width="0.6*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="12"/>
                        </Grid.ColumnDefinitions>
                        <ui:SimpleStackPanel Grid.Column="1" VerticalAlignment="Center">
                            <TextBlock Text="{Binding Source={StaticResource Loc}, Path=[Header_SimonStimulusPositionMode]}" FontSize="16"/>
                            <TextBlock Text="{Binding Source={StaticResource Loc}, Path=[Description_SimonStimulusPositionMode]}" FontSize="12"/>
                        </ui:SimpleStackPanel>
                        <ComboBox VerticalAlignment="Center" Width="180"
                                  ItemsSource ="{Binding StimulusPositionModes}"
                                  SelectedItem="{Binding SelectedStimulusPositionMode, Mode=TwoWay}"
                                  DisplayMemberPath="DisplayName"
                                  Grid.Column="3"
                                  HorizontalAlignment="Stretch"/>
                    </Grid>
                </Border>

                <Border CornerRadius="4" BorderBrush="Gray" BorderThickness="1">
                    <Grid  Height="80" >
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="12"/>
                            <ColumnDefinition Width="0.6*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="12"/>
                        </Grid.ColumnDefinitions>
                        <ui:SimpleStackPanel Grid.Column="1" VerticalAlignment="Center">
                            <TextBlock Text="{Binding Source={StaticResource Loc}, Path=[Header_SimonAnswerMode]}" FontSize="16"/>
                            <TextBlock Text="{Binding Source={StaticResource Loc}, Path=[Description_SimonAnswerMode]}" FontSize="12"/>
                        </ui:SimpleStackPanel>
                        <ComboBox VerticalAlignment="Center" Width="180"
                  ItemsSource ="{Binding AnswerModes}"
                  SelectedItem="{Binding SelectedAnswerMode, Mode=TwoWay}"
                  DisplayMemberPath="DisplayName"
                  Grid.Column="3"
                  HorizontalAlignment="Stretch"/>
                    </Grid>
                </Border>

                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="4" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="0.25*" />
                        <ColumnDefinition Width="16" />
                        <ColumnDefinition Width="0.25*" />
                        <ColumnDefinition Width="16" />
                        <ColumnDefinition Width="0.25*" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Row="0"
                               Grid.Column="0"
                               Text="{Binding Source={StaticResource Loc}, Path=[Header_TrialDuration]}" />
                    <TextBlock Grid.Row="0"
                               Grid.Column="2"
                               Text="{Binding Source={StaticResource Loc}, Path=[Header_FixationDuration]}" />
                    <TextBlock Grid.Row="0"
                               Grid.Column="4"
                               Text="{Binding Source={StaticResource Loc}, Path=[Header_MaxResponseTime]}" />
                    <ui:NumberBox Grid.Row="2"
                                  Grid.Column="0"
                                  Text="{Binding StimulusDuration}"
                                  Style="{StaticResource ModernReadOnlyNumberBoxStyle}"/>
                    <TextBlock Grid.Row="2"
                               Grid.Column="1"
                               VerticalAlignment="Center"
                               Text=" = "
                               HorizontalAlignment="Center"
                               FontSize="20" />
                    <ui:NumberBox Grid.Row="2"
                                  Grid.Column="2"
                                  Value="{Binding FixationDuration, Mode=TwoWay}"
                                  Minimum="1"
                  SpinButtonPlacementMode="Inline" />
                    <TextBlock Grid.Row="2"
                               Grid.Column="3"
                               VerticalAlignment="Center"
                               Text=" + "
                               HorizontalAlignment="Center"
                               FontSize="20"/>
                    <ui:NumberBox Grid.Row="2"
                                  Grid.Column="4"
                                  Value="{Binding MaxReactionTime, Mode=TwoWay}"
                                  Minimum="1"
                                  SpinButtonPlacementMode="Inline" />
                </Grid>
                <local:ProfileEditorCalculationModeView/>
            </ui:SimpleStackPanel>
        </ScrollViewer>
        <DockPanel Grid.Row="1"
                   VerticalAlignment="Bottom"
                   HorizontalAlignment="Center"
                   Margin="0,8,0,16">
            <ui:SimpleStackPanel Orientation="Horizontal"
                                 VerticalAlignment="Center"
                                 HorizontalAlignment="Center"
                                 Spacing="16">
                <Button Content="{Binding Source={StaticResource Loc}, Path=[Button_Cancel]}"
                        Style="{StaticResource OpaqueButtonStyle}"
                        Command="{Binding CancelCommand}" />
                <Button Content="{Binding Source={StaticResource Loc}, Path=[Button_Save]}"
                        Style="{StaticResource AccentButtonStyle}"
                        Command="{Binding SaveCommand}" />
            </ui:SimpleStackPanel>
        </DockPanel>
    </Grid>
</Window>

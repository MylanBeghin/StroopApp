<Page x:Class="StroopApp.Views.Configuration.SimonConfigurationPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:ui="http://schemas.modernwpf.com/2019"
      xmlns:local="clr-namespace:StroopApp.Views.Configuration"
      mc:Ignorable="d" 
      d:Background="#E0E0FF"
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ConfigurationPage">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="12" />
                <RowDefinition />
                <RowDefinition />
                <RowDefinition />
                <RowDefinition />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="0.3*"/>
                <ColumnDefinition Width="0.2*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Row="0"
                   Text="{Binding Source={StaticResource Loc}, Path=[Title_ExperimentDetails]}"
                   Style="{DynamicResource HeaderTextBlockStyle}"
                   HorizontalAlignment="Left"
                   Margin="0,0,0,12" />
            <ContentControl Grid.Row="2" Grid.Column="0" Grid.RowSpan="4" 
                        Content="{Binding ProtocolConfigurationViewModel}"/>
        </Grid>
    </ScrollViewer>
</Page>

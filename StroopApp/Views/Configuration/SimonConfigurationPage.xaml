<Page x:Class="StroopApp.Views.Configuration.SimonConfigurationPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:ui="http://schemas.modernwpf.com/2019"
      mc:Ignorable="d" 
      d:Background="WhiteSmoke"
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ConfigurationPage">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="12" />
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="12" />
                <RowDefinition Height="*" />
                <RowDefinition Height="12" />
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="12" />
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0"
                   Text="{Binding Source={StaticResource Loc}, Path=[Title_SimonConfigurationPage]}"
                   Style="{DynamicResource HeaderTextBlockStyle}"
                   HorizontalAlignment="Left"
                   Margin="0,0,0,12" />
            <ContentControl Grid.Row="1" 
                        Content="{Binding ProfileViewModel}"/>
            <Grid Grid.Row="3">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="0.7*"/>
                    <ColumnDefinition Width="12"/>
                    <ColumnDefinition Width="0.3*"/>
                </Grid.ColumnDefinitions>
                <ContentControl Grid.Column="0"
                                Content="{Binding SimonKeyMappingViewModel}" />
                <ContentControl Grid.Column="2"
                                Content="{Binding ExportFolderSeletorViewModel}" />
            </Grid>
            <ContentControl Grid.Row="5"
                            Content="{Binding ParticipantViewModel}"/>
            <ui:SimpleStackPanel Grid.Row="7" HorizontalAlignment="Center" >
                <Button Content="{Binding Source={StaticResource Loc}, Path=[Button_LaunchExperiment]}"
                        Style="{DynamicResource AccentButtonStyle}"
                        Command="{Binding LaunchExperimentCommand}" />
            </ui:SimpleStackPanel>
        </Grid>
    </ScrollViewer>
</Page>

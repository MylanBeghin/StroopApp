<UserControl x:Class="StroopApp.Views.Experiment.Experimenter.Graphs.ColumnGraphView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF"
             xmlns:ui="http://schemas.modernwpf.com/2019"
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="800">
    <UserControl.Resources>
        <SolidColorBrush x:Key="LegendCorrectBrush"
                         Color="#5B2EFF" />
        <SolidColorBrush x:Key="LegendIncorrectBrush"
                         Color="#FFA600" />
    </UserControl.Resources>
    <Border Background="{DynamicResource SystemControlBackgroundAltMediumBrush}"
            CornerRadius="8"
            Padding="8"
            BorderThickness="0.5"
            BorderBrush="{DynamicResource SystemControlForegroundBaseLowBrush}">
        <Grid>
            <lvc:CartesianChart x:Name="Chart"
                                Series="{Binding ColumnSerie}"
                                XAxes="{Binding XAxes}"
                                YAxes="{Binding YAxes}"
                                TooltipPosition="Bottom"
                                LegendPosition="Hidden" />
            <Border Background="#66000000"
                    CornerRadius="4"
                    Padding="6"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Top"
                    Margin="4"
                    Panel.ZIndex="1">
                <ui:SimpleStackPanel Orientation="Vertical"
                                     Spacing="4">
                    <ui:SimpleStackPanel Orientation="Horizontal"
                                         Spacing="6">
                        <Rectangle Width="14"
                                   Height="14"
                                   RadiusX="3"
                                   RadiusY="3"
                                   Fill="{StaticResource LegendCorrectBrush}" />
                        <TextBlock Text="{Binding Source={StaticResource Loc}, Path=[Label_ResponseCorrect]}"
                                   Foreground="White"
                                   FontSize="14"/>
                    </ui:SimpleStackPanel>
                    <ui:SimpleStackPanel Orientation="Horizontal"
                                         Spacing="6">
                        <Rectangle Width="14"
                                   Height="14"
                                   RadiusX="3"
                                   RadiusY="3"
                                   Fill="{StaticResource LegendIncorrectBrush}" />
                        <TextBlock Text="{Binding Source={StaticResource Loc}, Path=[Label_ResponseIncorrect]}"
                                   Foreground="White"
                                   FontSize="14" />
                    </ui:SimpleStackPanel>
                </ui:SimpleStackPanel>
            </Border>
        </Grid>
    </Border>
</UserControl>

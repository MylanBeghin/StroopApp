<UserControl x:Class="StroopApp.Views.Experiment.Experimenter.Graphs.LiveReactionTimeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:scm="clr-namespace:System.ComponentModel;assembly=WindowsBase"
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="800">
    <UserControl.Resources>
        <CollectionViewSource x:Key="GroupedReactionPointsView"
                              Source="{Binding GroupAverages}">
            <CollectionViewSource.SortDescriptions>
                <scm:SortDescription PropertyName="EndTrial"
                                     Direction="Descending" />
            </CollectionViewSource.SortDescriptions>
        </CollectionViewSource>
    </UserControl.Resources>
    <Border Padding="12"
            Background="{DynamicResource SystemControlBackgroundAltMediumBrush}"
            BorderBrush="{DynamicResource SystemControlForegroundBaseLowBrush}"
            BorderThickness="0.5"
            CornerRadius="8">
        <DockPanel>
            <TextBlock DockPanel.Dock="Top"
                       Text="{Binding Source={StaticResource Loc}, Path=[Title_MeanByInterval]}"
                       Style="{DynamicResource TitleTextBlockStyle}"
                       Margin="0,0,0,8" />
            <DataGrid ItemsSource="{Binding Source={StaticResource GroupedReactionPointsView}}"
                      Background="{DynamicResource SystemControlBackgroundAltLowBrush}"
                      HeadersVisibility="Column"
                      IsReadOnly="True"
                      AutoGenerateColumns="False"
                      CanUserReorderColumns="False"
                      CanUserResizeColumns="False"
                      CanUserSortColumns="False"
                      SelectionMode="Single"
                      BorderThickness="0"
                      GridLinesVisibility="None">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="{Binding Source={StaticResource Loc}, Path=[Label_TrialRange]}"
                                        Binding="{Binding Range}"
                                        Width="*" />
                    <DataGridTextColumn Header="{Binding Source={StaticResource Loc}, Path=[Label_Mean]}"
                                        Binding="{Binding AverageDisplay}"
                                        Width="*" />
                    <DataGridTextColumn Header="{Binding Source={StaticResource Loc}, Path=[Label_CorrectPercentage]}"
                                        Binding="{Binding CorrectDisplay}"
                                        Width="*" />
                </DataGrid.Columns>
            </DataGrid>
        </DockPanel>
    </Border>
</UserControl>

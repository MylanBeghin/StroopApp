<UserControl x:Class="StroopApp.Views.Experiment.ExperimentProfileView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:ui="http://schemas.modernwpf.com/2019"
             xmlns:converters="clr-namespace:StroopApp.Converters"
             xmlns:local="clr-namespace:StroopApp.Views.Experiment"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <converters:MillisecondsToHoursConverter x:Key="MillisecondsToHoursConverter" />
        <converters:MillisecondsToMinutesConverter x:Key="MillisecondsToMinutesConverter" />
        <converters:MillisecondsToSecondsConverter x:Key="MillisecondsToSecondsConverter" />
    </UserControl.Resources>
    <Grid>
        <Border
 Background="{DynamicResource SystemControlBackgroundAltMediumBrush}"
 CornerRadius="8"
 Padding="16"
 BorderThickness="0.5"
 BorderBrush="{DynamicResource SystemControlForegroundBaseLowBrush}">
            <ui:SimpleStackPanel Spacing="10">
                <TextBlock Text="Avancement"
   Style="{DynamicResource TitleTextBlockStyle}"/>
            <ui:SimpleStackPanel x:Name="WordCountPanel" Spacing="12">
                <ui:SimpleStackPanel.Style>
                    <Style TargetType="ui:SimpleStackPanel">
                        <Setter Property="Visibility" Value="Collapsed"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding CalculationMode}" Value="WordCount">
                                <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </ui:SimpleStackPanel.Style>
                    <TextBlock Style="{DynamicResource TitleTextBlockStyle}">
                        <TextBlock.Text>
                            <MultiBinding StringFormat=" {0} / {1}">
                                <Binding Path="WordCount"/>
                                <Binding Path="WordCount"/>
                            </MultiBinding>
                        </TextBlock.Text>
                    </TextBlock>
                    <ProgressBar
                        x:Name="ProgressBar2"
                        Width="130"
                        Value="{Binding ElementName=ProgressValue, Path=Value, Mode=OneWay}" />
                </ui:SimpleStackPanel>
            <ui:SimpleStackPanel x:Name="TimeDurationPanel" Spacing="12">
                <ui:SimpleStackPanel.Style>
                    <Style TargetType="ui:SimpleStackPanel">
                        <Setter Property="Visibility" Value="Collapsed"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding CalculationMode}" Value="TaskDuration">
                                <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </ui:SimpleStackPanel.Style>
                <TextBlock Text="WOrdCount"/>
            </ui:SimpleStackPanel>
            </ui:SimpleStackPanel>
        </Border>
    </Grid>
</UserControl>

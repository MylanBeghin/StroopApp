<UserControl x:Class="StroopApp.Views.Experiment.ExperimentProgressView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:ui="http://schemas.modernwpf.com/2019"
             xmlns:converters="clr-namespace:StroopApp.Converters"
             xmlns:local="clr-namespace:StroopApp.Views.Experiment"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <converters:MillisecondsToHoursConverter x:Key="MillisecondsToHoursConverter" />
        <converters:MillisecondsToMinutesConverter x:Key="MillisecondsToMinutesConverter" />
        <converters:MillisecondsToSecondsConverter x:Key="MillisecondsToSecondsConverter" />
    </UserControl.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Border
         Background="{DynamicResource SystemControlBackgroundAltMediumBrush}"
         CornerRadius="8,0,0,8"
         Padding="16"
         BorderThickness="0.5"
         BorderBrush="{DynamicResource SystemControlForegroundBaseLowBrush}">       
            <ui:SimpleStackPanel Grid.Column="0" Orientation="Vertical" HorizontalAlignment="Left" Spacing="10" >
                <TextBlock Text="Avancement" Style="{DynamicResource TitleTextBlockStyle}"/>
                <ui:SimpleStackPanel Grid.Column="0" Orientation="Horizontal" HorizontalAlignment="Left" >
                    <TextBlock Text="{Binding CurrentTrialNumber}" FontSize="16" Foreground="Black" Margin="5,0"/>
                    <TextBlock Text=" / " FontSize="16" Foreground="Black" Margin="5,0"/>
                    <TextBlock Text="{Binding TotalTrials}" FontSize="16" Foreground="Black" Margin="5,0"/>
                    <ui:ProgressBar Value="{Binding Progress, Mode=OneWay}" Width="130"/>
                </ui:SimpleStackPanel>
            </ui:SimpleStackPanel>
        </Border>
    </Grid>
</UserControl>
